# Copy to client/.env for local development. Only used by `npm run dev`
# (vite build) - in production the client is built into the server's
# Docker image, see the root Dockerfile.

# Same value as server/.env's GOOGLE_CLIENT_ID - needed here too, to
# render the Google Sign-In button.
VITE_GOOGLE_CLIENT_ID=218852498673-jmqu45f8ao463v7jdretmadimln5h4eb.apps.googleusercontent.com
